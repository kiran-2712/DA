#include <stdio.h>
int main() {
 int a[5]={1,3,5,7,9}, l=0, h=4, m , x=7;
 while(l<=h) {
    m=(l+h)/2;
    if(a[m]==x){ printf("Found at %d", m); return 0; }
    else if(a[m]<x) l=m+1;
    else h=m-1;
    }
    printf("Not found");
    return 0;
  }  
